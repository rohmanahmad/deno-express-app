'use strict'

import body from './body.ts'
import queries from './queries.ts'
import paths from './paths.ts'
import forms from './forms.ts'

const prefix = 'account'

export default { prefix, components: { body, queries, paths, forms } }
